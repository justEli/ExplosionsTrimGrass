main: '${project.groupId}.${project.name}'
name: '${project.name}'
version: '${project.version}'
authors: [JustEli]
website: '${project.url}'
api-version: '${versions.minecraft.spigot}'
folia-supported: true
